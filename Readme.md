# Shape & Contour Analyzer

A Streamlit application to detect geometric shapes in images, count objects,
and compute area and perimeter using OpenCV.

## Features
- Shape detection (Circle, Square, Rectangle, Polygon)
- Contour-based feature extraction
- Interactive Streamlit dashboard

## Run Locally
```bash
pip install -r requirements.txt
streamlit run app.py
